import { CalculatorLayout } from "@/components/calculator/templates/calculatorLayout";

export const metadata = {
    title: "Calculator | Core Desk",
};

export default function Home() {
    return <CalculatorLayout />
}
